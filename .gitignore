# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local bootstrap output (sdist + setup.sh staged for /custom-cont-init.d)
bootstrap/

# Per-user local tool overrides (machine-specific paths, not shared)
.claude/settings.local.json

# Local test harness (not part of the distributed package)
test-run/

# Local docker deployment (contains .env secrets, staged tarball, consume dir)
deploy/

# e2e stub consume dir (generated test input, not part of the repo)
tests/e2e/consume/

# e2e service cache (downloaded PP-OCRv6 models, not part of the repo)
tests/e2e/paddlex-cache/

# Local session scratch (not committed)
.superpowers/
